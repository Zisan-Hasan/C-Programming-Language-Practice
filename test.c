#include <stdio.h>
//#include <math.h>

int main()
{
    int x = 2 ,y = 7;

    int z = pow(x,y);

    int p = 28;
    float ans = sqrt(p);
    printf("square root of %d is %f\n",p,ans);


    //printf("Base = %d,power = %d\n Answer = %d\n", x, y, z);
}
